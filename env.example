FERNET_KEY=
# Generate via Python:
# python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# Optional: override DB URL (compose will set by default)
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/otpdb

# Ports (host side)
# Change these to run on different host ports
FRONTEND_PORT=8080
BACKEND_PORT=8000

# CORS allowed origins (comma-separated). Usually not needed when using nginx proxy.
CORS_ORIGINS=http://localhost:${FRONTEND_PORT}
